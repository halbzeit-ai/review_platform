# Development environment configuration for GPU processing

# Shared filesystem mount path
SHARED_FILESYSTEM_MOUNT_PATH=/mnt/dev-shared

# Database connection - use development database
DATABASE_HOST=65.108.32.143
DATABASE_PORT=5432
DATABASE_NAME=review_dev
DATABASE_USER=dev_user
DATABASE_PASSWORD=!dev_Halbzeit1024

# Or use full DATABASE_URL if preferred
DATABASE_URL=postgresql://dev_user:!dev_Halbzeit1024@65.108.32.143:5432/review_dev

# Database model configuration - DO NOT skip, use database like production
# SKIP_DB_MODEL_CONFIG=false  # Commented out to use database

# Fallback models if database is unavailable
DEFAULT_VISION_MODEL=gemma2:2b
DEFAULT_TEXT_MODEL=gemma2:2b
DEFAULT_SCORING_MODEL=phi3:mini

# Production server URL (for callbacks)
PRODUCTION_SERVER_URL=http://65.108.32.143

# Processing configuration
PROCESSING_DEVICE=cuda
MAX_PROCESSING_TIME=600
INCLUDE_DEBUG_INFO=true